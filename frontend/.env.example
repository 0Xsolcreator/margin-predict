# Google sign-in (Sui zkLogin via Enoki). Get the API key from the Enoki portal
# (enoki.mystenlabs.com) and the Google OAuth client ID from Google Cloud Console.
# Wallet connect works without these; only Google sign-in needs them.
REACT_APP_ENOKI_API_KEY=
REACT_APP_GOOGLE_CLIENT_ID=
